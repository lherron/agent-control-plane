/** Scheduler state panel shell — phase 2 will add data fetching. */
export function SchedulerStatePanel() {
  return (
    <div className="border border-border rounded-md bg-card p-3 text-sm text-muted">
      Scheduler state — not loaded
    </div>
  )
}
